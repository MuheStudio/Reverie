[pytest]
minversion = 8.0
addopts = --import-mode=importlib --strict-config --strict-markers
testpaths =
    tests
norecursedirs =
    .git
    .hypothesis
    .pytest_cache
    frontend
    N.E.K.O
    venv
filterwarnings =
    ignore::DeprecationWarning
