export const ENABLE_LOCALES = ['zh', 'en', 'pt', 'es', 'ja'];

// Chinese-first product: the UI boots in zh even when the browser locale is
// something else. FALLBACK_LNG only covers keys missing from a locale.
export const DEFAULT_LNG = 'zh';

export const FALLBACK_LNG = 'en';
