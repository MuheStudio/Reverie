# macOS arm64 测试 DMG 验收摘要

验收日期：2026-09-21。用户已确认原独立核心应用人工验收通过；本摘要记录随后完成的源码及 DMG 自动验证，不扩大人工验收范围。

## 候选与交付

- 版本：0.5.5；应用：Reverie macOS Test；标识：com.muhe.reverie.macos.test。
- 应用保持原 ad-hoc 签名，DMG 未签名，均未公证。DMG 仅封装已验收应用副本，没有重建或重签应用。
- 镜像：`Reverie-macOS-Test-0.5.5-arm64-56e011339749.dmg`，183533146 字节（175.0 MiB），UDZO / HFS+。
- DMG SHA-256：`39d39c0672e46b612a5048dd29c992e9dc14695227078d38aca5c6369769db60`。
- 原应用源码快照：`7fa80f2fc9969de2321790ab4e9fdccb4107f12646fae2d4aae27081f310ac89`。
- 原签名应用文件清单摘要：`56e01133974962314375f559d658d6a9b012413b4594610d5381a035e7a5e70f`。

分支提交新增了应用包之外的 DMG 工具、验收器、测试与文档。已逐项核对与原快照的差异，包内执行代码和运行锁没有变化；最终镜像内及复制后的应用均与原成功构建清单一致。提交标识与原 dirty 工作树快照属于不同层次的记录，不能互相替代。

构建和验证入口：

```bash
./script/package-macos-dmg.sh
./script/check-macos-dmg.sh
```

相同输入再次运行构建入口通过完整校验后复用镜像，SHA-256 不变；未升级依赖或改变锁文件。应用、DMG、原始日志和本机路径清单不进入 Git。完整布局、路径和重建说明见 [macOS 打包说明](MACOS-PACKAGING.md)。

## 环境与源码检查

实机：macOS 26.6.2（25G83），arm64。系统默认 Python 前后为 3.14.6。应用内为独立 CPython 3.12.14 / 20260901，加 47 个哈希锁定的运行依赖；没有开发虚拟环境或系统解释器回退。

构建工具：Node 24.19.0、pnpm 11.7.0、uv 0.12.17、Electron 42.5.2、electron-builder 26.15.6。DMG 使用 builder 配套的 dmg-builder 1.2.5 / 75c8a6c 工具归档，SHA-256 写入独立工具清单；该工具自身的 Python 3.14.2 仅供制作镜像，不进入应用。

最终完整源码检查 13 步通过：957 项 pytest（另有 1 项既有 memory-eval xfail 和 1 条既有 collection warning）、385 项 Node、262 项 Vitest，以及两组 TypeScript、Vite 构建、V4 桥接和核心桌面冒烟。未为验收新增关键测试跳过或放宽生产时限。

修复了跨平台测试的路径/符号链接夹具，原始 vendor JS 用 Git 属性保持字节以满足上游哈希。新增真实进程组超时清理、磁盘写满时继续清理、挂载失败、镜像/清单损坏及原生错误框路径边界测试。

## 最终镜像与复制后应用

最终 DMG 的完整测试用时 52.072 秒，通过以下检查：

- Finder 图标视图中应用、Applications 快捷方式及中文安装说明完整可见，说明位置已针对较矮标签页调整。只读挂载确认不可写，Applications 链接严格指向 /Applications。
- 实际在最终 DMG 内启动、完成 V4 鉴权及本地模拟模型 UI 聊天；随后复制至仓库外的中文与空格路径，推出镜像后连续启动/正常退出 3 次。
- 沿用同一隔离测试目录，消息内容及 ID 全部恢复，受保护密钥未替换；复制后的恢复没有再次调用模型。真实 macOS Keychain/safeStorage 链路通过，未导出秘密。
- 镜像内和复制后的包内 Python、89 个 arm64 Mach-O、98 项签名及 123 个动态库加载上下文均通过审计。
- 两处均实际验证 SQLCipher 4.12.0 与 sqlite-vec 0.1.9 的加密建库、向量查询、跨进程重开；错误密钥及普通 SQLite 读取被拒绝。
- 损坏 vault、损坏数据库、数据库存在但 vault 缺失三类测试均未鉴权，原测试数据未被重置；缺失 vault 时没有生成替代密钥。
- 原生错误框由核对 PID、应用路径/标识及完整文字的测试辅助工具确认；最终轮无需外部补救或强制清理。
- 镜像和应用前后摘要不变；无残留进程、挂载，临时测试数据与 helper 文件已清理；未写入真实 Applications 目录或已有用户数据。

坏数据库测试的退出码 0 是拒绝后执行普通 Quit 的结果，不表示存储启动成功；损坏或缺失 vault 确认错误框后退出 1。

启动阶段耗时如下，单位毫秒。主进程、renderer 与 V4 为主进程阶段累计时刻；取钥和 Python 探测为操作耗时；最后一列为外部观察到可用的时间。

| 场景 | 主进程 ready | 取钥 | Python 探测 | renderer | V4 鉴权 | 外部可用 |
|---|---:|---:|---:|---:|---:|---:|
| 镜像内启动 | 75 | 5 | 50 | 316 | 1422 | 1720 |
| 推出镜像后第 1 次启动 | 70 | 0 | 51 | 310 | 1324 | 1577 |
| 推出镜像后第 2 次启动 | 54 | 1 | 47 | 293 | 1242 | 1485 |
| 推出镜像后第 3 次启动 | 59 | 0 | 47 | 295 | 1228 | 1477 |

生产解释器预检和桥接握手时限仍各为 20 秒；辅助测试器等待错误框的时限不属于生产启动时限。这些是当前实机和缓存状态的结果，不是所有机器冷启动保证。

## 失败记录与未验证项

本轮同步占位文件曾导致工具读取和加密探针等待超时。仅恢复已有文件本地内容：实际定向请求并完整验证 6,993 个文件，最终相关目录 dataless 为 0；没有重装或升级环境。早期失败记录保留在忽略目录，随后使用原测试和原时限全量通过。

初版错误框辅助工具仅识别旧应用包验收路径，已为新的隔离安装测试路径补充严格匹配和边界测试。最终成功轮使用修订后的工具重新完整验证。旧布局镜像另行留档，不作为本次最终产物。

- 后续 CI 修复未改动 Windows 安装器配置、运行锁或应用运行代码；Windows CI 已通过，详见下节。Windows 人工 UI 验收仍未完成。
- 未验证 Developer ID、公证、下载隔离/Gatekeeper、跨版本或跨签名钥匙串访问。最低系统声明为 macOS 14，实机仅覆盖上述系统。
- 未扩展 Intel/Universal、自动更新、Live2D 模型、GPT-SoVITS、付费模型、完整首次引导及平台 UI 覆盖。
- 未下载 embedding 模型；核心候选仍记录可选 Live2D 资源缺失及语义检索不可用，并使用原有有界词法检索。这不代表完整语义检索验收通过，也不涉及明文存储回退。
- 本轮仅本地构建和分支提交；不创建 PR、合并 main、创建/推送发布标签或发布 Release。

## 分支推送与 Windows CI 复核

2026-09-21 获得写权限后，已正常推送到 [origin/codex/Mac](https://github.com/MuheStudio/Reverie/tree/codex/Mac)，未强推。最终完整 [Windows quality gate](https://github.com/MuheStudio/Reverie/actions/runs/35550488514) 成功，受检代码提交为 `4e3a1e5091cc3d862a5e13beff78bb8cb9455e44`；随后仅补充本验收文档。

| 检查 | Windows runner 实测结果 |
|---|---|
| Python 3.12.10 第一方测试 | 957 passed、1 项既有 xfail、1 条既有 warning；695.37 秒 |
| Node 测试 | 363 passed、28 个 macOS/POSIX 等平台限定跳过、0 failed |
| Vitest | 34 个文件、262 项通过 |
| TypeScript / Vite | 类型检查和生产构建通过 |
| Windows 应用目录构建 | 固定生产 Python 与运行依赖构建成功 |
| 包内桥接冒烟 | V4 stdio 握手成功，4 个数据库确认加密，runtimeDegraded=false |
| SPDX SBOM | 生成并作为本次 workflow artifact 上传，未附加到 Release |

首次 CI 暴露了新增测试子进程在 Windows 默认编码下输出中文失败，以及 Reflex 首次写入的 40 秒超时。已显式使用 UTF-8；仅为 Windows Reflex 首次写入 184 条完整耐久化种子记录设置 120 秒测试预算，并增加 stderr 线程诊断。没有缩减数据或断言，也没有更改应用加密参数和生产握手时限。最终该测试实测 14.20 秒，首轮超时的具体阻塞点不能从旧日志确定。

随后修复了既有 CI 的 pnpm 缓存顺序、显式构建解释器路径，以及包验证器把 `ambient.py`、`backup.py` 误当目录的问题；必需文件、禁止遗漏模块、错误类型与链接检查仍然执行。CI 自动触发规则未改。相关追加验证在 macOS 上为 403 项 Node 和 262 项 Vitest 通过；原始失败及成功日志保留在本机忽略目录。

以上追加仅涉及测试、CI 和包验证器。原 macOS 应用及 DMG 没有重建或重签，DMG SHA-256 仍为 `39d39c0672e46b612a5048dd29c992e9dc14695227078d38aca5c6369769db60`。Windows CI 成功不等同于 Windows 人工 UI 或正式分发验收。
